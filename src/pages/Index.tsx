import React from 'react';
import { HomePage } from '@/features/landing';

/**
 * Index page - renders the landing home page
 */
const Index: React.FC = () => {
  return <HomePage />;
};

export default Index;
